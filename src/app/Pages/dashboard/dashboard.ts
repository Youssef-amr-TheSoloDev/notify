import { Component, inject } from '@angular/core';
import { StatCardComponent } from '../../component/stat-card/stat-card';
import { IconComponent } from '../../component/icon/icon';
import { NotifyDataService } from '../../services/notify-data.service';

@Component({ selector: 'app-dashboard', standalone: true, imports: [StatCardComponent, IconComponent], template: `<section class="page dashboard"><header class="page-head"><div><p class="eyebrow">Tuesday, May 21, 2025</p><h1>Dashboard</h1></div><app-icon name="sun"/></header><div class="stats"><app-stat-card [value]="data.notes().length" label="Notes" tone="blue" icon="notes" link="/notes"/><app-stat-card [value]="data.tasks().length" label="Tasks" tone="green" icon="tasks" link="/tasks"/><app-stat-card [value]="data.alerts().length" label="Alerts" tone="purple" icon="alerts" link="/alerts"/><app-stat-card [value]="3" label="Due Soon" tone="yellow" icon="clock" link="/tasks"/></div><section class="panel activity"><div class="section-title"><h2>Recent Activity</h2><a>View all</a></div>@for (item of data.activities; track item.title) {<div class="activity-row"><span class="activity-icon {{item.type}}"><app-icon [name]="item.type === 'note' ? 'notes' : item.type === 'task' ? 'tasks' : 'alerts'"/></span><div><strong>{{item.title}}</strong><small>{{item.detail}}</small></div><time>{{item.age}}</time><span class="tag {{item.type}}">{{item.type === 'note' ? 'Note' : item.type === 'task' ? 'Task' : 'Alert'}}</span></div>}</section></section>`, styles: [`.page{max-width:1000px;margin:0 auto;padding:var(--space-page-y) var(--space-page-x) 50px}.page-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:20px}.page-head h1{font:700 20px 'Space Grotesk',sans-serif;margin:0}.eyebrow{color:var(--color-muted);font-size:10px;margin:0 0 5px}.page-head>app-icon{color:var(--color-muted)}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:var(--space-3)}.panel{background:var(--color-surface);border:1px solid var(--color-border-soft);border-radius:10px;box-shadow:0 5px 18px var(--shadow-soft)}.activity{margin-top:16px;padding:16px 14px}.section-title{display:flex;justify-content:space-between;align-items:center}.section-title h2{font:600 13px 'Space Grotesk',sans-serif;margin:0 0 10px}.section-title a{font-size:10px;color:var(--color-blue)}.activity-row{display:flex;align-items:center;gap:var(--space-control);border-top:1px solid var(--color-divider);padding:var(--space-control) 0}.activity-icon{width:25px;height:25px;border-radius:50%;display:grid;place-items:center}.activity-icon.note{background:var(--color-blue-pale);color:var(--color-blue)}.activity-icon.task{background:var(--color-green-soft);color:var(--color-green)}.activity-icon.alert{background:var(--color-purple-soft);color:var(--color-purple)}.activity-row div{min-width:0;flex:1}.activity-row strong,.activity-row small{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.activity-row strong{font-size:10px}.activity-row small,time{color:var(--color-muted);font-size:9px}.tag{font-size:9px;padding:4px 8px;border-radius:10px}.tag.note{color:var(--color-blue);background:var(--color-blue-soft)}.tag.task{color:var(--color-green);background:var(--color-green-soft)}.tag.alert{color:var(--color-purple);background:var(--color-purple-soft)}@media(max-width:700px){.page{padding:var(--space-page-y) 14px}.stats{grid-template-columns:repeat(2,1fr);gap:9px}.activity-row time{display:none}}`] })
export class DashboardPage { readonly data = inject(NotifyDataService); }
